apply plugin: "java"

version = "1.0"

allprojects {
    version = rootProject.version
    task hello {
        doLast { task ->
            println "I'm $task.project.name"
        }
    }
}

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.13'
}